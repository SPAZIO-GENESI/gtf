id: EVD-cicd-staging-runs
title: Run pubblici della CI imgauth (check + deploy staging + smoke)
location:
  system: "GitHub Actions, repo imgauth (pubblico)"
  ref: "workflow CI (.github/workflows/ci.yml) — job check e deploy-staging"
  public_via: "https://github.com/SPAZIO-GENESI/imgauth/actions/workflows/ci.yml"
produced_by: >
  Collaudo end-to-end del 2026-07-11 (P24 FASE 3): push diretto su main
  (check + deploy-staging verdi), PR normale con modifica banale (solo
  check gira, deploy-staging correttamente skipped), merge della PR (deploy
  staging automatico + smoke verdi), PR con errore di sintassi introdotto
  ad arte (check fallisce, PR chiusa senza merge). Ogni run è pubblico e
  ispezionabile da chiunque sul repo GitHub. Esteso alla FASE 4 (stesso
  giorno): run pubblico che attraversa l'intero gate di produzione
  (attesa approvazione → upload → promozione → smoke), primo rilascio
  gated della storia del progetto
  (https://github.com/SPAZIO-GENESI/imgauth/actions/runs/29153958055).
  Esteso alla FASE 7 (stesso giorno): collaudo browser reale (Playwright)
  dell'interfaccia di staging pubblicata su attestazione-staging, flusso
  completo scegli→attesta→PDF→verifica confermato dal vivo.
collection: auto
last_seen: 2026-07-21
integrity: "raccolta settimanale (collect-evidence.mjs, riuso dei dati già raccolti per MET-governance/P32, zero fetch in più): prova solo che i run di ci.yml su imgauth/main sono pubblicamente visibili via API GitHub — verificabile anche a mano aprendo la tab Actions del repo pubblico imgauth, dove ogni run mostra i singoli step con esito"
supports: [CTL-cicd-pipeline]
visibility: public
