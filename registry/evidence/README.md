# evidence/ — Evidenze (EVD)

Un file `EVD-<slug>.yaml` per ogni evidenza: dove vive, chi la produce, come
si verifica, con che freschezza massima resta valida. Validato da
`schemas/evidence.schema.json`. Vedi GTF-ARCH §6 (Evidence Plane).

Popolato in fase **M1 — Import dell'esistente** (vedi ARCHITECTURE.md §12).
