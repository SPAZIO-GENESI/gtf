id: IMP-imgauth-conventions
title: Convenzioni per dominio email — pool mensile, tetto individuale, degrado, admin CRUD + report
repo: imgauth
ref: "worker.js — matchConvention, wiring in handleDevOAuthCallback (quota/label/expires_at/convention_id), authenticateAgent (SELECT convention_id/owner_email, check expires_at esteso a entrambi i kind), handleHash (contabilità pool/tetto, log convention_attestations, fascia/fascia_motivo/convenzione in risposta), peekAgentCredential + handlePdf (tier sidecar), writeCertMeta (tier/convention_id), handleAdminConventionsList/Create/Update/Report, handleAdminKeysUpdate (forget esteso a convention_attestations); schema/conventions.sql (tabelle conventions, convention_attestations)"
supports: [CTL-convention-accounting]
visibility: public
